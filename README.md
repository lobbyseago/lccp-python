# lccp-python

Python implementation of **Lobster Configuration and Command-Line Parsing (LCCP)**.

This package follows the normative behavior defined in https://github.com/lobbyseago/lccp-spec.

It supports config discovery, precedence resolution, CLI parsing, and provenance-aware debugging.

Intended for publication to PyPI.
